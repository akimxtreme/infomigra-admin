<%= form_for(situations_arrivals_country) do |f| %>
  <% if situations_arrivals_country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(situations_arrivals_country.errors.count, "error") %> prohibited this situations_arrivals_country from being saved:</h2>

      <ul>
      <% situations_arrivals_country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :percentage_profile %>
    <%= f.text_field :percentage_profile %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
