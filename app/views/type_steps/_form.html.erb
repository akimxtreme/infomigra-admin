<%= form_for(type_step) do |f| %>
  <% if type_step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_step.errors.count, "error") %> prohibited this type_step from being saved:</h2>

      <ul>
      <% type_step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
