<%= form_for(country_category) do |f| %>
  <% if country_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country_category.errors.count, "error") %> prohibited this country_category from being saved:</h2>

      <ul>
      <% country_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :percentage_profile %>
    <%= f.text_field :percentage_profile %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
