<%= form_for(level_study) do |f| %>
  <% if level_study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(level_study.errors.count, "error") %> prohibited this level_study from being saved:</h2>

      <ul>
      <% level_study.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
