<%= form_for(commune) do |f| %>
  <% if commune.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commune.errors.count, "error") %> prohibited this commune from being saved:</h2>

      <ul>
      <% commune.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :provinces_id %>
    <%= f.text_field :provinces_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
