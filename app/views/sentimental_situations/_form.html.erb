<%= form_for(sentimental_situation) do |f| %>
  <% if sentimental_situation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sentimental_situation.errors.count, "error") %> prohibited this sentimental_situation from being saved:</h2>

      <ul>
      <% sentimental_situation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
