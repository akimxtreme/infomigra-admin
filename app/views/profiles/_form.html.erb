<%= form_for(profile) do |f| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_categories_id %>
    <%= f.text_field :country_categories_id %>
  </div>

  <div class="field">
    <%= f.label :situations_arrivals_countries_id %>
    <%= f.text_field :situations_arrivals_countries_id %>
  </div>

  <div class="field">
    <%= f.label :current_statuses_id %>
    <%= f.text_field :current_statuses_id %>
  </div>

  <div class="field">
    <%= f.label :family_bonds_id %>
    <%= f.text_field :family_bonds_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
