<%= form_for(user_step) do |f| %>
  <% if user_step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_step.errors.count, "error") %> prohibited this user_step from being saved:</h2>

      <ul>
      <% user_step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :users_id %>
    <%= f.text_field :users_id %>
  </div>

  <div class="field">
    <%= f.label :steps_id %>
    <%= f.text_field :steps_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
