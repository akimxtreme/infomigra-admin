<%= form_for(province) do |f| %>
  <% if province.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(province.errors.count, "error") %> prohibited this province from being saved:</h2>

      <ul>
      <% province.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :num_communes %>
    <%= f.number_field :num_communes %>
  </div>

  <div class="field">
    <%= f.label :regions_id %>
    <%= f.text_field :regions_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
