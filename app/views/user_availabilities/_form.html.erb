<%= form_for(user_availability) do |f| %>
  <% if user_availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_availability.errors.count, "error") %> prohibited this user_availability from being saved:</h2>

      <ul>
      <% user_availability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :abilities_id %>
    <%= f.text_field :abilities_id %>
  </div>

  <div class="field">
    <%= f.label :users_id %>
    <%= f.text_field :users_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
