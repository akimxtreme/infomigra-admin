<%= form_for(family_bond) do |f| %>
  <% if family_bond.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(family_bond.errors.count, "error") %> prohibited this family_bond from being saved:</h2>

      <ul>
      <% family_bond.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :slug %>
    <%= f.text_field :slug %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :percentage_profile %>
    <%= f.text_field :percentage_profile %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
